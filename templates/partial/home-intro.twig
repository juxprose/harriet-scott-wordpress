<div class="from-gray-100 bg-gradient-to-b py-10">
  <div class="container max-w-screen-lg mx-auto">
    <div class="flex items-start">
      <div class="flex-1 p-10 -mr-5 relative z-5 from-gray-200 bg-gradient-to-b">
        <h1 class="text-4xl mb-3">{{ options.global_intro__title }}</h1>
        <h2 class="text-2xl">{{ options.global_intro__subtitle }}</h2>
        <p class="my-5 mr-16">{{ options.global_intro__text }}</p>
        <a href="{{ options.global_intro__link }}">More about me</a>
      </div>
      <div class="flex-1 mt-5">
        <img class="shadow-lg" src="{{ Image(options.global_intro__image).src }}" />
      </div>
    </div>
  </div>
</div>